# Supabase (leave blank to run the API in STUB mode with no database)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=

# Set to false once the real ML pipeline is wired (Phase 3)
STUB_MODE=true

# Comma-separated origins for Member 5's React dev server / Vercel deploy
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
