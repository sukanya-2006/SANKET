[pytest]
# Collect only the real suite. backend/app/ contains ad-hoc scripts named test_*.py
# (manual LLM spot-checks) that pytest would otherwise import and fail on — they use
# relative paths and expect to be run by hand from backend/app/.
testpaths = backend/tests
